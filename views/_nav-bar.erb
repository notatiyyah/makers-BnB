<nav class="navbar navbar-expand-lg navbar-light">
  <div class="header">
    <img class="img-responsive" src="<%= url('/images/logo.jpg') %>" >
    <br>
  </div>
  <ul class="nav justify-content-end ml-auto">
    <li id="last-nav-item" class="nav-item">
      <a class="nav-link" href="#">About</a>
    </li>
    <li id="last-nav-item" class="nav-item">
      <% if @logged_in.nil? %>
        <a class="nav-link" href="/sessions/new">Sign In</a>
      <% else %>
        <a class="nav-link" href="/sessions/log_out" formaction="post">Sign Out</a>
      <% end %>
    </li>
  </ul>
</nav>
<% flash.keys.each do |type| %>
  <div class="alert alert-<%=  type == :warning ? "warning" : "success"%>" role="alert">
    <p class="alert-link"><%= flash[type] %></p>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>
<br>